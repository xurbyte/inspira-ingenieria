/**
 * Precision UI — barrel export for all shared homepage components.
 *
 * Import everything from a single entry point:
 *   import { ScrollReveal, SmoothScrollLink, GridOverlay } from "@/components/precision-ui";
 */

export { ScrollReveal } from "./scroll-reveal";
export { SmoothScrollLink } from "./smooth-scroll-link";
export { GridOverlay, AxisLine, CornerBracket } from "./structural";
